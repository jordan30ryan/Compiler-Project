program allocarr is
    integer a[0:5];
    integer b[-1:2];
    global integer ga[0:5];
    global integer gb[-1:2];
begin
    a[0] := 1;
    a[1] := 2;
    a[2] := 3;
    putinteger(a[0]);
    putinteger(a[1]);
    putinteger(a[2]);

    b[-1] := -5;
    b[1] := 5;
    putinteger(b[-1]);
    putinteger(b[1]);
end program.

